<div class="logo">
  <h1>Time Tracker</h1>
</div>

<% if current_user %>
  <ul class="nav nav-tabs custom-nav">
    <li class="<%= active?(root_path) %>">
      <%= link_to "", root_path, class: 'glyphicon glyphicon-home' %>
    </li>

    <%= render 'shared/employee_nav_items' if employee? %>

    <%= render 'shared/audit_log_tab' if admin? %>

    <li class="nav-item dropdown pull-right">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown">Options<strong class="caret"></strong></a>
      <ul class="dropdown-menu">

        <%= render 'shared/admin_dropdown' if admin? %>

        <li>
          <%= link_to "Logout", destroy_user_session_path, method: :delete %>
        </li>
      </ul>
    </li>
  </ul>
<% end %>
